{
  "name": "@repo/zustand",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "check-types": "tsc --noEmit",
    "lint": "eslint src/ --max-warnings 0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "@repo/common": "*",
    "typescript": "^5"
  },
  "dependencies": {
    "immer": "^10.1.1",
    "peerjs": "^1.5.4",
    "zustand": "^5.0.3"
  }
}
